"""
calendar_tools.py — REMOVED.

Cal.com booking integration has been removed from this project. Booking
requests captured during calls are now sent as Telegram notifications
(see notify.notify_booking_request) for the team to confirm manually —
no external calendar API is called.

This file is intentionally left as an empty stub in case anything still
imports it; it exposes no functions.
"""
